package com.contentintelligence.platform.summary;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ContentSummaryRepository extends JpaRepository<ContentSummary, String> {
}
